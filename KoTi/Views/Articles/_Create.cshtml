@model CreateContentViewModel

<dialog id="create-article-dialog">
    <header>
        <h2>Add New Article</h2>
        <nav>
            <button type="button" class="koti-btn primary with-indicator" id="create-article-submit-button"
                    onclick="$id('create-article-form').requestSubmit()">
                <span id="create-article-indicator" class="spinner htmx-indicator"></span>
                <i class="bi bi-plus-lg"></i> Create
            </button>
            <button type="button" class="koti-btn cancel-btn"
                    onclick="this.closest('dialog').close()">Cancel</button>
        </nav>
    </header>
    <form id="create-article-form"
          hx-post hx-controller="Articles" hx-action="Create" hx-route-language="@Model.Language"
          hx-target="closest dialog" hx-swap="outerHTML" hx-trigger="submit"
          hx-disabled-elt="#create-article-submit-button" hx-indicator="#create-article-indicator">
        <fieldset class="formbox">
            <input type="hidden" asp-for="Language"/>
            
            <div>
                <label for="create-article-name">Name (URL slug):</label>
                <input id="create-article-name" class="input-block" required autocomplete="please-dont" asp-for="Name"></input>
                @Html.ValidationMessageFor(m => m.Name)
            </div>

            <div>
                <label for="create-article-title">Title</label>
                <input id="create-article-title" class="input-block heading-font" style="font-size: 30px;" required autocomplete="off" asp-for="Title" articleholder="Title"/>
                @Html.ValidationMessageFor(m => m.Title)
            </div>
        </fieldset>
    </form>
</dialog>