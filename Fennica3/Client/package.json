{
  "name": "fennica3-js",
  "version": "3.4",
  "private": true,
  "license": "GPL-3.0-or-later",
  "packageManager": "bun@1.3.9",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "bun run typecheck && bun run build.js",
    "dev": "bun run build.js --dev",
    "watch": "bun run --watch build.js --dev"
  },
  "dependencies": {
    "glider-js": "^1.7.9",
    "leaflet": "^1.9.4",
    "proj4leaflet": "^1.0.2"
  },
  "devDependencies": {
    "@types/glider-js": "^1.7.12",
    "@types/proj4leaflet": "^1.0.12",
    "typescript": "^5.9.3"
  }
}
